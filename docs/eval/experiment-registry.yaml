registry_version: "1"

# 在任何 dev 候选运行之前，复制并填写下方模板，然后移入 experiments。
# 不在此文件中登记虚构实验、结果或简历数字。sealed test 的访问事件单独写入
# append-only JSONL registry，且不提交 token。
#
# - experiment_id: "rag-v1-hybrid-rrf"
#   dataset_version: "rag-v1"
#   status: preregistered
#   baseline_variant: "vector-only"
#   baseline_config_sha256: "<64-char-lowercase-sha256>"
#   frozen_evidence:
#     corpus_sha256: "<snapshot-only 输出的 64 位 sha256>"
#     chunk_manifest_sha256: "<snapshot-only 输出的 64 位 sha256>"
#     vector_artifact_sha256: "<snapshot-only 输出的 64 位 sha256>"
#   primary_metric: "ndcg_at_k"
#   direction: "higher"
#   minimum_effect: 0.0          # 实验前明确填写可接受的最小实际提升
#   bootstrap:
#     iterations: 5000
#     confidence_level: 0.95
#     seed: 7
#   guardrails:
#     - metric: "answerability_f1"
#       direction: "higher"
#       max_regression: 0.0      # 实验前明确填写
#   candidates:
#     - variant_id: "hybrid-rrf"
#       commit: "<frozen-git-commit>"
#       config_sha256: "<64-char-lowercase-sha256>"

# 建议流程：先运行 --strict --snapshot-only 冻结 live 输入，再将输出 hash
# 和 baseline/candidate 配置 hash 写入本文件，最后执行正式 dev experiment。
experiments: []
