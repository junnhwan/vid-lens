import type { Metadata } from 'next'

export const metadata: Metadata = { title: '配置与常见问题 · 映知文档' }

export default function DocsConfigPage() {
  return (
    <article className="docs-article">
      <h1>配置与常见问题</h1>
      <p className="docs-lede">模型服务怎么配、地址怎么填，以及使用中常见问题的处理办法。</p>

      <h2 id="ai-profiles">AI 服务配置</h2>
      <p>
        映知不绑定特定模型供应商，需要在「设置 → AI 服务」中按能力接入自己的服务。一份配置（配置档）包含四组能力：
      </p>
      <ul>
        <li><strong>对话（必需）</strong>：生成摘要与问答回答，使用 OpenAI 兼容的聊天接口。</li>
        <li><strong>语音识别 ASR（必需）</strong>：把视频语音转成文字。</li>
        <li><strong>向量 Embedding（必需）</strong>：生成检索索引，需要填写维度（如 1024 / 1536）。</li>
        <li><strong>视觉（可选）</strong>：为关键帧生成画面描述；关闭时仍可使用本地 OCR。</li>
      </ul>
      <p>
        密钥在服务端加密保存，只用于发起模型调用，不会在页面、导出文件或日志中明文出现。同一份配置还可以调整 Agent
        执行预算（工具调用次数、时长、输入输出与视觉帧上限）。
      </p>

      <h3 id="base-url">服务地址怎么填</h3>
      <p>不同能力对地址的要求不同，表单会按能力给出示例并校验常见错误：</p>
      <ul>
        <li><strong>对话 / 语音识别 / 视觉</strong>：填「服务基础地址」，系统在其后拼接各自的接口路径。例如硅基流动填 <code>https://api.siliconflow.cn/v1</code>。</li>
        <li><strong>向量 Embedding</strong>：填「完整接口地址」，系统原样请求，不再拼接路径。例如 <code>https://api.siliconflow.cn/v1/embeddings</code>。</li>
        <li>部分服务商（如 DeepSeek 的对话接口）允许直接使用主域名；以各服务商文档为准。</li>
      </ul>
      <p>
        填错路径（漏填 <code>/v1</code>、重复 <code>/v1</code>、把完整接口填成基础地址、Embedding 漏掉{' '}
        <code>/embeddings</code>）时，表单会直接提示。保存前可以用「拉模型」读取可用模型列表，并用逐能力探测实际调用所选模型做一个小样本检查（会产生少量供应商费用，页面会先说明）。
      </p>

      <h3 id="profile-transfer">配置导入 / 导出</h3>
      <p>
        配置可以导出为 JSON 文件（含地址、模型、维度、预算，<strong>不含明文或脱敏密钥</strong>），在其他环境导入后重新填写密钥即可。导入默认创建新配置，不会覆盖已有配置。
      </p>

      <h2 id="faq">常见问题</h2>

      <h3 id="上传之后为什么视频没有开始处理">上传之后为什么视频没有开始处理？</h3>
      <p>
        上传只负责入库。请打开视频详情手动点「开始转写」；之后的摘要、索引也都在详情页按需启动。这样可以让你先确认视频和配置，再决定花哪些模型额度。
      </p>

      <h3 id="为什么生成摘要按钮不可点">为什么「生成摘要」按钮不可点？</h3>
      <p>
        摘要需要先有转写；同一视频同一时间只执行一个处理任务。如果索引正在排队或构建，摘要会显示需要等待当前任务结束，按钮旁有原因说明。索引失败而任务已结束时，摘要仍可提交。
      </p>

      <h3 id="摘要为什么会分段">摘要为什么会分段？</h3>
      <p>
        转写能放进对话模型的输入预算时，摘要只请求模型一次；超出时按顺序分段并合并。在「设置 → AI 服务」填写服务实际支持的「模型上下文窗口」后，新任务会按这个上限选择。留空时使用保守默认值。不要把窗口填得高于服务实际限制，否则单次请求可能因超限失败；正在续跑的任务保留原分段计划。
      </p>

      <h3 id="建立索引有什么用不做会怎样">建立索引有什么用？不做会怎样？</h3>
      <p>
        索引用于语义检索和视频问答：建立后，问答可以按内容含义找到相关转写片段并定位视频位置。只播放视频、看转写或摘要时不需要索引。建立索引会把已有转写和画面文字发给你配置的向量模型，消耗 Embedding 额度；不会重新转写，也不会改动原内容。详见{' '}
        <a href="/docs/features#index">功能说明 · 检索索引</a>。
      </p>

      <h3 id="问答提示检索降级--无引用是什么意思">问答提示「检索降级 · 无引用」是什么意思？</h3>
      <p>
        表示这一轮检索没有完成（常见原因是向量模型额度被其他任务临时占用，自动等待后仍未成功），系统改用已有的摘要 / 转写直接生成回答。这种回答没有检索引用，可信度不同于正常检索结果，请稍后重试或检查向量模型的额度与限流配置。
      </p>

      <h3 id="摘要失败怎么排查">摘要失败，怎么排查？</h3>
      <p>
        失败卡片会标明类别（网络错误、超时、上游 5xx / 504、限流、鉴权失败等）和当前重试状态，并附可复制的诊断编号。把诊断编号交给维护者，即可在后台日志中关联到具体任务、模型与上游请求。连续多次失败通常指向模型服务或网络链路问题，本地重试无法修复上游故障。
      </p>

      <h3 id="索引报错维度不一致如期望-1536-维实际-1024-维">索引报错维度不一致（如期望 1536 维、实际 1024 维）？</h3>
      <p>
        这是配置的向量维度与已建向量列不一致导致的。当前版本启动时会自动把旧的定长向量列转为未定长列，并在检索时按模型与维度过滤；升级后重建索引即可。配置中的「维度」应与所选 Embedding 模型的实际输出一致，可用表单中的探测功能确认。
      </p>

      <h3 id="长视频后半段没有画面-ocr--描述">长视频后半段没有画面 OCR / 描述？</h3>
      <p>
        画面证据有抽帧数量上限。当前版本的抽帧预算会覆盖全片片头到片尾，详情页显示证据的实际覆盖范围；旧任务若是在早期版本处理的，需要重新转写才会按新策略补采。关闭过「后续生成画面证据」的视频也不会新增画面证据。详见{' '}
        <a href="/docs/features#visual">功能说明 · 画面证据</a>。
      </p>

      <h3 id="演示账号能做什么">演示账号能做什么？</h3>
      <p>演示账号为只读：可以浏览界面和已有内容，但不能修改配置、启动处理或更改记忆设置。</p>
    </article>
  )
}
