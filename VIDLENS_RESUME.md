# VidLens 项目简历迭代稿

> 当前版本：v2
> 适用方向：后端开发 / AI 应用开发
> 维护方式：后续持续在本文档中迭代，并在版本记录中保留每次简历稿变化。

## v2 综合投递版

### 映知 —— 长视频理解与 Agent 知识库问答平台

GitHub：`github.com/junnhwan/vid-lens`

技术栈：Go、Gin、GORM、PostgreSQL/pgvector、Redis、RabbitMQ、MinIO、FFmpeg、Next.js

项目简介：一个面向长视频内容理解的知识库与智能问答平台，打通语音、画面与 Agent 问答链路，支持单视频和知识库范围内的引用式回答与回放。

- 引入 RabbitMQ 将 ASR、视觉分析和 RAG 索引拆为可重试异步阶段，避免长耗时处理阻塞请求，实现阶段级故障恢复。
- 采用 300 秒核心窗口 + 5 秒重叠分片并行转写，结合结果拼接与分片复用，解决跨边界截断、重复及重复处理问题。
- 将转写、OCR 与画面描述统一映射到视频时间轴，构建带来源的多模态知识片段，支持时间定位与引用回放。
- 基于 pgvector 融合关键词与语义检索，结合结果融合、上下文扩展与排序，增强复杂问题的证据覆盖能力。
- 将 Agent 执行限制在授权视频、工具白名单与多维预算内，持久化运行状态，避免越权访问和无界调用。
- 实现按时间窗口触发的视觉调查，抽取原始画面并可选调用 VLM，补足字幕无法表达的视觉信息。
- 采用 Redis 管理分片进度、锁与重复任务，结合 MinIO 实现断点续传和对象合并。

## 版本记录

### v1

- 确定“技术动作 + 解决问题 + 结果”的表达结构。
- 明确使用 RabbitMQ，不使用 Kafka 表述。
- 项目亮点覆盖异步任务、ASR、视频视觉、多模态 RAG、Agent 和 Redis/MinIO。
- 暂不加入未经真实评测或压测验证的准确率、延迟和吞吐指标。

### v2

- 删除“流式展示、失败收尾”等偏交互和过程性的表述，Agent 亮点聚焦作用域、工具白名单、预算约束和越权防护。
- 保持每条亮点以核心技术动作和可解释产出为主，减少实现细节堆叠。
